![docker1](https://github.com/user-attachments/assets/babcaaf1-b2d1-4662-8f8d-d85db1207e43)
![docker2](https://github.com/user-attachments/assets/3e4936c1-5658-4b9a-9a9e-1131ff73ad5e)

# format
$ docker build -t <컨테이너 이미지 이름> .

# 컨테이너 이미지 생성
$ docker build -t simple-spring-boot-app .


## 🚀 컨테이너 실행 명령어 상세 설명

| 옵션 | 설명 |
| :--- | :--- |
| **docker run** | 도커 이미지를 컨테이너로 생성하고 실행합니다. |
| **-d** | 컨테이너를 백그라운드(Background) 모드로 실행합니다. (터미널을 계속 쓸 수 있음) |
| **--name** | 컨테이너의 이름을 지정합니다. (예: `simple-spring-boot-app`) |
| **-p [호스트]:[컨테이너]** | 내 컴퓨터(로컬)와 컨테이너 내부의 포트를 연결합니다. (예: `8080:8080`) |
| **이미지 이름** | 실행할 도커 이미지의 이름입니다. (명령어의 맨 마지막에 위치) |


# format
$ docker run -d --name <컨테이너 이름> -p 8080:8080 <실행할 이미지 이름>

# 컨테이너 생성 및 실행
$ docker run -d --name simple-spring-boot-app -p 8080:8080 simple-spring-boot-app
